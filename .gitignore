# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
terraform.tfvars
*.auto.tfvars
backend.tf

# .terraform.lock.hcl is deliberately NOT ignored. Committing it is what makes
# a run from another machine resolve the same provider versions.

# Local
.DS_Store
